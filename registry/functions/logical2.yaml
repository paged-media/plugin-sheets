# sheet.fn.logical.* (M1 additions) — logical family. Schema: see
# math.yaml. Rust kernels under sheet-fn/src/families/logical2.rs (the M1
# logical track). All rows status: planned. Provenance: ECMA-376
# §18.17.7 + public Microsoft function docs.

- id: sheet.fn.logical.ifs
  name: IFS
  family: logical
  arity: { min: 2, max: null }
  volatility: none
  range_aware: false
  rust: logical2::ifs
  status: planned
  provenance: ["Microsoft IFS (public docs) — variadic (condition, value) pairs; min one pair"]
  tests:
    rust: ["sheet-conformance/tests/fn_logical.rs::sheet_fn_logical_ifs"]
    corpus: ["corpus/fn-corpus/logical/ifs.golden.tsv"]

- id: sheet.fn.logical.switch
  name: SWITCH
  family: logical
  arity: { min: 3, max: null }
  volatility: none
  range_aware: false
  rust: logical2::switch
  status: planned
  provenance: ["Microsoft SWITCH (public docs) — expression, then variadic (value, result) pairs, optional default"]
  tests:
    rust: ["sheet-conformance/tests/fn_logical.rs::sheet_fn_logical_switch"]
    corpus: ["corpus/fn-corpus/logical/switch.golden.tsv"]

- id: sheet.fn.logical.xor
  name: XOR
  family: logical
  arity: { min: 1, max: null }
  volatility: none
  range_aware: true
  rust: logical2::xor
  status: planned
  provenance: ["ECMA-376 §18.17.7 (XOR) — variadic logical exclusive-or"]
  tests:
    rust: ["sheet-conformance/tests/fn_logical.rs::sheet_fn_logical_xor"]
    corpus: ["corpus/fn-corpus/logical/xor.golden.tsv"]

- id: sheet.fn.logical.ifna
  name: IFNA
  family: logical
  arity: { min: 2, max: 2 }
  volatility: none
  range_aware: false
  rust: logical2::ifna
  status: planned
  provenance: ["Microsoft IFNA (public docs) — value, value_if_na"]
  tests:
    rust: ["sheet-conformance/tests/fn_logical.rs::sheet_fn_logical_ifna"]
    corpus: ["corpus/fn-corpus/logical/ifna.golden.tsv"]
