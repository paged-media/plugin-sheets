# Rust
/target

# clean-room mounts — never committed (NOT mounted in T0; see CLAUDE.md)
references/

# TS
node_modules/
dist/
*.tsbuildinfo

# wasm-bindgen build outputs (regenerated by scripts/build-wasm.sh;
# bin/.gitkeep keeps the directory)
packages/sheet-bundle/bin/*.wasm
packages/sheet-bundle/bin/*.js
packages/sheet-bundle/bin/*.d.ts

# oracle venv (LibreOffice harness tooling, if ever local)
.venv/
